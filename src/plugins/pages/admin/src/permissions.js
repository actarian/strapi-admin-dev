const permissions = {
  accessMain: [{ action: 'plugin::pages.pages.read', subject: null }],
  create: [{ action: 'plugin::pages.pages.create', subject: null }],
  delete: [{ action: 'plugin::pages.pages.delete', subject: null }],
  update: [{ action: 'plugin::pages.pages.update', subject: null }],
  read: [{ action: 'plugin::pages.pages.read', subject: null }],
};

export default permissions;
