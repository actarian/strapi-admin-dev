'use strict';

const page = require('./page/page.controller');
const slug = require('./slug/slug.controller');
const seo = require('./seo/seo.controller');

module.exports = {
  page,
  slug,
  seo,
};
